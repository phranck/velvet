schemaVersion: 1
repository:
  owner: example
  name: status
statusPage:
  name: Example Status
services:
  - name: API
    checks:
      - name: Application health
        url: https://api.example.com/health
        jsonAssertions:
          - path: /status
            equals: ok
          - path: /dependencies/database
            equals: true
