schemaVersion: 1
repository: { owner: example, name: status }
statusPage: { name: Example Status, theme: velvet }
services:
  - name: Website
    checks:
      - name: Origin
        url: https://example.com
        expectedStatusCodes: [700]
