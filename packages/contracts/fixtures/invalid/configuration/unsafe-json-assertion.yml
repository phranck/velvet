schemaVersion: 1
repository: { owner: example, name: status }
statusPage: { name: Example Status, theme: velvet }
services:
  - name: API
    checks:
      - name: Health
        url: https://api.example.com/health
        jsonAssertions:
          - { path: /constructor/prototype/ready, equals: true }
