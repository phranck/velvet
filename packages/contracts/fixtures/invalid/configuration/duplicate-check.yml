schemaVersion: 1
repository: { owner: example, name: status }
statusPage: { name: Example Status }
services:
  - name: API
    checks:
      - { id: health, name: Health, url: https://api.example.com/health }
      - { id: health, name: Readiness, url: https://api.example.com/ready }
