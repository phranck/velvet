/**
 * What the mockups render from, which is now one of the fixtures.
 *
 * The fixture directory is where the data lives, because the gallery, the
 * conformance suite and the screenshot workflow all render the same cases and a
 * second copy would be a second thing to keep true. This file stays as the name
 * the mockups already import, and goes with them.
 */

export {
  GENERATED_AT,
  incidentsDocument,
  mockConfig,
  responseTimesDocument,
  statusDocument,
} from "../bundles/fixtures/velvet-underground.js";
