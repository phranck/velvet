# A divided column of coloured segments, with two limbs enclosing what they report.

name = "NCC-1701-D"
description = "A divided column of coloured segments carrying the service names, two limbs enclosing the notices and the readings, and a table of events rather than a stack of cards."
era = "2364"
version = "1.0.0"

# Where it stands in the list an operator chooses from, lowest first.
order = 4

# A withdrawn theme keeps every installation already published in it and is
# offered to nobody new.
state = "offered"

# The shape of the status data this theme understands.
dataVersion = 1

layouts = ["grouped"]
readings = "overlay"

# The element this theme's page is rooted at. What an operator sets is written
# onto it, so their value stands where this theme's own declarations stand and
# wins over them rather than being inherited into a losing position.
root = ".ncc-1701-d-page"

# The colours the picture of this page is drawn with, which somebody sees when
# they share its address. The card is rasterised without a browser, so it cannot
# read this stylesheet and is told instead. This theme draws its services on the
# black it stands on, so the surface takes no line of its own.
[card]
backgroundStart = "#000000"
backgroundEnd = "#000000"
surface = "#000000"
surfaceEdge = "#33334a"
operational = "#99ccff"
degraded = "#ff9c00"
outage = "#dc4a5f"
noData = "#171310"
ipv4 = "#ff9c00"
ipv6 = "#cc99cc"
textPrimary = "#ffcc99"
textSecondary = "#cc99cc"
textTertiary = "#9c9cff"
clouds = false

[entries]
template = "template.ts"
styles = "theme.css"
script = "script.ts"
