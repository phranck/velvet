{
  "name": "@velvet/monitor-action",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "pretest": "bun run --filter @velvet/contracts build && bun run --filter @velvet/monitor build && bun run --filter @velvet/github-incidents build",
    "test": "bun test test/*.test.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@velvet/contracts": "workspace:*",
    "@velvet/github-incidents": "workspace:*",
    "@velvet/monitor": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
