{
  "name": "velvet",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "site"
  ],
  "scripts": {
    "build": "npm run build --workspace @velvet/contracts && npm run build --workspace @velvet/upptime-adapter && npm run build --workspace @velvet/site",
    "test": "npm run test:lockfile && npm run test --workspace @velvet/contracts && npm run test --workspace @velvet/upptime-adapter",
    "test:lockfile": "node --test tests/lockfile.test.mjs",
    "typecheck": "npm run typecheck --workspace @velvet/contracts && npm run typecheck --workspace @velvet/upptime-adapter"
  },
  "engines": {
    "node": ">=22"
  }
}
