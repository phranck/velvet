// Generated by scripts/build-release.ts. Do not edit by hand.
// Velvet 1.0.0 from phranck/velvet-template@631df18e2c7cae526be9f1f0f7b838ad87a26bd4.

export const VELVET_RELEASE = {
  "manifest": {
    "schemaVersion": 1,
    "version": "1.0.0",
    "releaseType": "feature",
    "automaticInstallEligible": false,
    "template": {
      "repository": "phranck/velvet-template",
      "commit": "631df18e2c7cae526be9f1f0f7b838ad87a26bd4"
    },
    "compatibility": {
      "minimumInstalledVersion": "1.0.0",
      "configurationSchemaVersion": 1,
      "dataSchemaVersion": 1,
      "configurationMigrationRequired": false,
      "dataMigrationRequired": false
    },
    "managedFiles": [
      {
        "path": ".github/ISSUE_TEMPLATE/config.yml",
        "strategy": "replace",
        "sourcePath": ".github/ISSUE_TEMPLATE/config.yml",
        "sha256": "5a02cdb0d8bb19eb478a2f1116c4946db0fa6bbd54f55136672b182c3df4cd53"
      },
      {
        "path": ".github/ISSUE_TEMPLATE/maintenance.yml",
        "strategy": "generate",
        "generator": "maintenance-issue-template-v1",
        "sourcePath": ".github/ISSUE_TEMPLATE/maintenance.yml",
        "sha256": "87d0b496d3d2d12c4cffe716e14b03beea0c1376c114069d9504a6afaf9bf091"
      },
      {
        "path": ".github/workflows/deploy-announce.yml",
        "strategy": "replace",
        "sourcePath": ".github/workflows/deploy-announce.yml",
        "sha256": "ad9f7509c22f5fb3999b76e1453b7d34bafe73dc1dcb650a3392e0fe283a88b5"
      },
      {
        "path": ".github/workflows/maintenance-switch.yml",
        "strategy": "generate",
        "generator": "maintenance-workflow-v1",
        "sourcePath": ".github/workflows/maintenance-switch.yml",
        "sha256": "27870ca587201fa3d51c23006a760539eb6460da4e2cc4e7ef328668d9a2270f"
      },
      {
        "path": ".github/workflows/velvet-response-times.yml",
        "strategy": "generate",
        "generator": "response-times-workflow-v1",
        "sourcePath": ".github/workflows/velvet-response-times.yml",
        "sha256": "a6bf543308343ec0bc62089224d55c9bcee7693e620bf53803b64cd13944c628"
      },
      {
        "path": ".github/workflows/velvet-status.yml",
        "strategy": "generate",
        "generator": "status-workflow-v1",
        "sourcePath": ".github/workflows/velvet-status.yml",
        "sha256": "71203029bfe43dc8d3d9e3f5f99add8d332e60d0b4bc2b6be8c8bf01de475abe"
      },
      {
        "path": ".github/workflows/velvet-update-check.yml",
        "strategy": "replace",
        "sourcePath": ".github/workflows/velvet-update-check.yml",
        "sha256": "12db12115df70fbce8b263901cb2d234fa1860496d71631cb78a63ea8295351b"
      },
      {
        "path": ".github/workflows/velvet.yml",
        "strategy": "generate",
        "generator": "pages-workflow-v1",
        "sourcePath": ".github/workflows/velvet.yml",
        "sha256": "372e32f9fe6a46c436ef95245548cf640942d8c526a1dd25197886b87120380f"
      },
      {
        "path": "velvet.lock.json",
        "strategy": "generate",
        "generator": "version-lock-v1"
      }
    ],
    "releaseNotes": "# Velvet 1.0.0\n\nVelvet monitors your services from GitHub Actions and publishes a status page through GitHub Pages, without a server or a database.\n\n## What it does\n\n- Direct IPv4 checks of every configured endpoint, every five minutes, with separate response-time samples four times a day.\n- Incidents and planned maintenance recorded as GitHub Issues, opened and closed automatically after confirmed failures and recoveries.\n- Up to 365 days of availability, response-time, incident, and maintenance history on a dedicated branch the monitor owns alone.\n- A themeable status page built from that history, with four system themes and detailed visual configuration.\n\n## Installing and updating\n\nBrowser onboarding at [setup.velvet.li](https://setup.velvet.li/onboarding/) creates the repository, writes the configuration, enables Pages, and starts monitoring. It is the only supported way in, and the only one that records which Velvet version an installation runs.\n\nFrom then on Velvet installs new versions for you. An update replaces only the workflow and Issue-template files Velvet owns, plus its own version lock. Your configuration, history, incidents, secrets, `README.md`, and `LICENSE` are never part of one, and a check running in your own repository refuses any update that would touch them.\n\n## Notes\n\nIPv6 monitoring is deliberately absent until GitHub-hosted runners provide documented native support. A configured service is monitored over IPv4 only.\n"
  },
  "sources": {
    ".github/ISSUE_TEMPLATE/config.yml": "blank_issues_enabled: false\ncontact_links:\n  - name: Velvet support\n    url: https://github.com/phranck/velvet/issues\n    about: Report problems with the monitor or status page.\n",
    ".github/ISSUE_TEMPLATE/maintenance.yml": "name: Planned maintenance\ndescription: Schedule maintenance and show it in the status history.\ntitle: \"[Maintenance] \"\nlabels:\n  - maintenance\nbody:\n  - type: markdown\n    attributes:\n      value: Velvet keeps monitoring during maintenance. Select only the services and checks covered by this work.\n  - type: dropdown\n    id: affected-targets\n    attributes:\n      label: Affected services and checks\n      description: Update these choices when you add or rename services in velvet.yml.\n      multiple: true\n      options:\n        - Website (all checks) [website/*]\n        - Website / Website [website/website]\n    validations:\n      required: true\n  - type: input\n    id: starts-at\n    attributes:\n      label: Starts at\n      description: Use an ISO 8601 timestamp with a time zone, for example 2026-08-01T22:00:00+02:00.\n      placeholder: \"2026-08-01T22:00:00+02:00\"\n    validations:\n      required: true\n  - type: input\n    id: ends-at\n    attributes:\n      label: Ends at\n      description: Use an ISO 8601 timestamp with a time zone, for example 2026-08-01T23:30:00+02:00.\n      placeholder: \"2026-08-01T23:30:00+02:00\"\n    validations:\n      required: true\n  - type: textarea\n    id: summary\n    attributes:\n      label: Summary\n      description: Briefly describe the planned work.\n      placeholder: Application and database updates.\n    validations:\n      required: true\n",
    ".github/workflows/deploy-announce.yml": "# Application repositories may announce deploy_start and deploy_end events.\n# The service slug must match a service ID in velvet.yml.\nname: Deploy announce\n\non:\n  repository_dispatch:\n    types: [deploy_start, deploy_end]\n\npermissions:\n  contents: write\n  issues: write\n\nconcurrency:\n  group: velvet-status-data\n  cancel-in-progress: false\n\njobs:\n  announce:\n    if: github.repository != 'phranck/velvet-template'\n    runs-on: ubuntu-latest\n    steps:\n      - name: Update deploy maintenance\n        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3\n        with:\n          script: |\n            const type = context.payload.action;\n            const payload = context.payload.client_payload || {};\n            const service = String(payload.service || \"Service\")\n              .replace(/\\s+/gu, \" \")\n              .trim()\n              .slice(0, 128);\n            const slug = String(payload.slug || service)\n              .trim()\n              .toLowerCase()\n              .replace(/[^a-z0-9]+/g, \"-\")\n              .replace(/^-+|-+$/g, \"\");\n            if (!service) {\n              core.setFailed(\"The deploy service name is required.\");\n              return;\n            }\n            if (!/^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/.test(slug) || slug.length > 64) {\n              core.setFailed(\"The deploy service slug is invalid.\");\n              return;\n            }\n\n            const { owner, repo } = context.repo;\n            const marker = `<!-- deploy-maintenance:${slug} -->`;\n\n            if (type === \"deploy_start\") {\n              const label = \"maintenance\";\n              try {\n                await github.rest.issues.getLabel({ owner, repo, name: label });\n              } catch (error) {\n                if (error.status !== 404) throw error;\n                try {\n                  await github.rest.issues.createLabel({\n                    owner,\n                    repo,\n                    name: label,\n                    color: \"fbca04\",\n                    description: \"Velvet planned maintenance\",\n                  });\n                } catch (createError) {\n                  if (createError.status !== 422) throw createError;\n                }\n              }\n              const requestedMinutes = Number(payload.minutes);\n              const minutes = Number.isFinite(requestedMinutes) && requestedMinutes > 0\n                ? Math.min(1440, Math.floor(requestedMinutes))\n                : 15;\n              const start = new Date();\n              const end = new Date(start.getTime() + minutes * 60000);\n              const body = [\n                \"### Affected services and checks\",\n                \"\",\n                `- ${service} (all checks) [${slug}/*]`,\n                \"\",\n                \"### Starts at\",\n                \"\",\n                start.toISOString(),\n                \"\",\n                \"### Ends at\",\n                \"\",\n                end.toISOString(),\n                \"\",\n                \"### Summary\",\n                \"\",\n                `Automated deploy in progress for **${service}**.`,\n                \"\",\n                marker,\n              ].join(\"\\n\");\n              await github.rest.issues.create({\n                owner,\n                repo,\n                title: `[Maintenance] Deploying ${service}`,\n                labels: [label],\n                body,\n              });\n            } else {\n              const openIssues = await github.paginate(\n                github.rest.issues.listForRepo,\n                { owner, repo, state: \"open\", labels: \"maintenance\", per_page: 100 },\n              );\n              for (const issue of openIssues) {\n                if ((issue.body || \"\").includes(marker)) {\n                  await github.rest.issues.update({\n                    owner,\n                    repo,\n                    issue_number: issue.number,\n                    state: \"closed\",\n                  });\n                }\n              }\n            }\n\n            await github.rest.repos.createDispatchEvent({\n              owner,\n              repo,\n              event_type: \"velvet-monitor\",\n            });\n",
    ".github/workflows/maintenance-switch.yml": "# The planned-maintenance Issue Form is the normal path. This manual Action is\n# a compact fallback for opening a maintenance window from the Actions tab.\nname: Maintenance switch\n\non:\n  workflow_dispatch:\n    inputs:\n      message:\n        description: \"What is happening\"\n        required: true\n      services:\n        description: \"Service IDs, comma-separated\"\n        required: true\n        default: \"website\"\n      minutes:\n        description: \"Expected duration in minutes\"\n        required: false\n        default: \"30\"\n\npermissions:\n  contents: write\n  issues: write\n\nconcurrency:\n  group: velvet-status-data\n  cancel-in-progress: false\n\njobs:\n  open:\n    if: github.repository != 'phranck/velvet-template'\n    runs-on: ubuntu-latest\n    steps:\n      - name: Open planned maintenance\n        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3\n        with:\n          script: |\n            const inputs = context.payload.inputs || {};\n            const selected = [...new Set(\n              String(inputs.services || \"\")\n                .split(\",\")\n                .map((value) => value.trim().toLowerCase())\n                .filter(Boolean),\n            )];\n            const invalid = selected.filter(\n              (serviceId) =>\n                serviceId.length > 64 ||\n                !/^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/.test(serviceId),\n            );\n            if (selected.length === 0 || selected.length > 32 || invalid.length > 0) {\n              core.setFailed(\n                selected.length > 32\n                  ? \"Select no more than 32 service IDs.\"\n                  : invalid.length > 0\n                  ? `Invalid service IDs: ${invalid.join(\", \")}`\n                  : \"Select at least one service ID.\",\n              );\n              return;\n            }\n\n            const requestedMinutes = Number(inputs.minutes);\n            const minutes = Number.isFinite(requestedMinutes) && requestedMinutes > 0\n              ? Math.min(1440, Math.floor(requestedMinutes))\n              : 30;\n            const message = String(inputs.message || \"\")\n              .replace(/\\s+/gu, \" \")\n              .trim()\n              .slice(0, 200);\n            if (!message) {\n              core.setFailed(\"A maintenance message is required.\");\n              return;\n            }\n\n            const start = new Date();\n            const end = new Date(start.getTime() + minutes * 60000);\n            const targets = selected.map(\n              (serviceId) => `- ${serviceId} (all checks) [${serviceId}/*]`,\n            );\n            const body = [\n              \"### Affected services and checks\",\n              \"\",\n              ...targets,\n              \"\",\n              \"### Starts at\",\n              \"\",\n              start.toISOString(),\n              \"\",\n              \"### Ends at\",\n              \"\",\n              end.toISOString(),\n              \"\",\n              \"### Summary\",\n              \"\",\n              message,\n              \"\",\n              \"Close this issue to end the maintenance window early.\",\n            ].join(\"\\n\");\n            const { owner, repo } = context.repo;\n            const label = \"maintenance\";\n            try {\n              await github.rest.issues.getLabel({ owner, repo, name: label });\n            } catch (error) {\n              if (error.status !== 404) throw error;\n              try {\n                await github.rest.issues.createLabel({\n                  owner,\n                  repo,\n                  name: label,\n                  color: \"fbca04\",\n                  description: \"Velvet planned maintenance\",\n                });\n              } catch (createError) {\n                if (createError.status !== 422) throw createError;\n              }\n            }\n            await github.rest.issues.create({\n              owner,\n              repo,\n              title: `[Maintenance] ${message}`,\n              labels: [label],\n              body,\n            });\n            await github.rest.repos.createDispatchEvent({\n              owner,\n              repo,\n              event_type: \"velvet-monitor\",\n            });\n",
    ".github/workflows/velvet-response-times.yml": "name: Velvet response times\n\non:\n  schedule:\n    - cron: \"0 0,6,12,18 * * *\"\n  workflow_dispatch: {}\n\npermissions:\n  contents: write\n\nconcurrency:\n  group: velvet-status-data\n  cancel-in-progress: false\n\njobs:\n  monitor:\n    if: github.repository != 'phranck/velvet-template'\n    name: Sample response times\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out status repository\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n      - name: Publish Velvet response times\n        uses: phranck/velvet/actions/monitor@197fced9c68928bf26d35d0ddac13da388eb562d\n        with:\n          mode: response\n        # Map the same explicitly configured header secrets as the status job.\n",
    ".github/workflows/velvet-status.yml": "name: Velvet status\n\non:\n  push:\n    branches: [main]\n    paths:\n      - \"velvet.yml\"\n      - \".github/workflows/velvet-status.yml\"\n  schedule:\n    - cron: \"*/5 * * * *\"\n  workflow_dispatch: {}\n  repository_dispatch:\n    types: [velvet-monitor]\n  issues:\n    types: [opened, closed, reopened, edited, labeled, unlabeled]\n\npermissions:\n  contents: write\n  issues: write\n\nconcurrency:\n  group: velvet-status-data\n  cancel-in-progress: false\n\njobs:\n  monitor:\n    if: github.repository != 'phranck/velvet-template' && (github.event_name != 'push' || github.event.before != '0000000000000000000000000000000000000000') && (github.event_name != 'issues' || github.event.label.name == 'maintenance' || contains(github.event.issue.labels.*.name, 'maintenance'))\n    name: Check services\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out status repository\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n      - name: Publish Velvet status\n        uses: phranck/velvet/actions/monitor@197fced9c68928bf26d35d0ddac13da388eb562d\n        with:\n          mode: status\n        # Map only explicitly configured header secrets here, for example:\n        # env:\n        #   API_HEALTH_TOKEN: ${{ secrets.API_HEALTH_TOKEN }}\n",
    ".github/workflows/velvet-update-check.yml": "# Verifies a managed Velvet update in the repository it will change, before the\n# update service is allowed to merge it. The service proves the same ownership\n# rules on its side; this runs in your repository, on your own runner, so the\n# guarantee holds even if the service is wrong about what it is merging.\nname: Velvet update check\n\non:\n  pull_request:\n\npermissions:\n  contents: read\n\njobs:\n  verify:\n    # Only Velvet's own update branches are judged here, so an unrelated pull\n    # request in this repository is never affected by these rules.\n    if: startsWith(github.head_ref, 'velvet/update/')\n    # This name is what the update service waits for. Changing it stops every\n    # update from merging rather than failing, so it must stay as it is.\n    name: Velvet update check\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out the merge result\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n        with:\n          fetch-depth: 0\n      - name: Verify the update changes only Velvet-owned files\n        env:\n          HEAD_REF: ${{ github.head_ref }}\n        run: |\n          set -euo pipefail\n\n          # Velvet owns exactly these paths. Everything else in this repository\n          # belongs to you, including velvet.yml, README.md, and LICENSE.\n          owned=$(cat <<'PATHS'\n          .github/ISSUE_TEMPLATE/config.yml\n          .github/ISSUE_TEMPLATE/maintenance.yml\n          .github/workflows/deploy-announce.yml\n          .github/workflows/maintenance-switch.yml\n          .github/workflows/velvet-response-times.yml\n          .github/workflows/velvet-status.yml\n          .github/workflows/velvet-update-check.yml\n          .github/workflows/velvet.yml\n          velvet.lock.json\n          PATHS\n          )\n\n          # HEAD is the merge result GitHub built, and its first parent is the\n          # branch being merged into, so this diff is exactly what merging would\n          # change here.\n          changed=$(git diff --name-only HEAD^1 HEAD)\n          if [ -z \"$changed\" ]; then\n            echo \"::error::This update changes nothing.\"\n            exit 1\n          fi\n\n          failed=0\n          while IFS= read -r path; do\n            [ -n \"$path\" ] || continue\n            if printf '%s\\n' \"$owned\" | grep -Fxq -- \"$path\"; then\n              echo \"  ok        $path\"\n            else\n              echo \"::error file=$path::Velvet does not own this file and must not change it.\"\n              failed=1\n            fi\n          done <<< \"$changed\"\n          [ \"$failed\" -eq 0 ]\n\n          version=\"${HEAD_REF#velvet/update/}\"\n          locked=$(jq -r '.installedVersion' velvet.lock.json)\n          repository=$(jq -r '.template.repository' velvet.lock.json)\n          schema=$(jq -r '.schemaVersion' velvet.lock.json)\n          commit=$(jq -r '.template.commit' velvet.lock.json)\n\n          if [ \"$schema\" != \"1\" ]; then\n            echo \"::error::velvet.lock.json declares an unsupported schema version: $schema\"\n            exit 1\n          fi\n          if [ \"$repository\" != \"phranck/velvet-template\" ]; then\n            echo \"::error::velvet.lock.json points at an untrusted template: $repository\"\n            exit 1\n          fi\n          if ! printf '%s' \"$commit\" | grep -Eq '^[a-f0-9]{40}$'; then\n            echo \"::error::velvet.lock.json does not pin an immutable template revision.\"\n            exit 1\n          fi\n          if [ \"$locked\" != \"$version\" ]; then\n            echo \"::error::This branch installs $version but the lock records $locked.\"\n            exit 1\n          fi\n\n          echo \"Velvet $version verified against $repository@$commit.\"\n",
    ".github/workflows/velvet.yml": "# Build and deploy after the native monitor publishes a valid snapshot.\nname: Velvet Pages\n\non:\n  workflow_run:\n    workflows: [\"Velvet status\", \"Velvet response times\"]\n    types: [completed]\n  workflow_dispatch: {}\n\npermissions: {}\n\nconcurrency:\n  group: velvet-pages\n  cancel-in-progress: false\n\njobs:\n  monitor:\n    if: github.repository != 'phranck/velvet-template' && github.event_name == 'workflow_dispatch'\n    name: Check services and publish initial data\n    runs-on: ubuntu-latest\n    permissions:\n      contents: write\n      issues: write\n    steps:\n      - name: Check out status repository\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n      - name: Publish Velvet status\n        uses: phranck/velvet/actions/monitor@197fced9c68928bf26d35d0ddac13da388eb562d\n        with:\n          mode: status\n        # Map only explicitly configured header secrets here, for example:\n        # env:\n        #   API_HEALTH_TOKEN: ${{ secrets.API_HEALTH_TOKEN }}\n\n  build:\n    needs: monitor\n    if: github.repository != 'phranck/velvet-template' && always() && ((github.event_name == 'workflow_dispatch' && needs.monitor.result == 'success') || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success'))\n    name: Build status page\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      pages: write\n      id-token: write\n    steps:\n      - name: Check out the latest default branch\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n        with:\n          ref: ${{ github.event.repository.default_branch }}\n      - name: Check out Velvet data\n        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1\n        with:\n          ref: velvet-data\n          path: .velvet-data\n      - name: Build Velvet site\n        uses: phranck/velvet@197fced9c68928bf26d35d0ddac13da388eb562d\n        with:\n          config: velvet.yml\n          data: .velvet-data/velvet-data/v1\n          output: velvet-dist\n      - name: Configure Pages\n        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d\n      - name: Upload artifact\n        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9\n        with:\n          path: velvet-dist\n\n  deploy:\n    needs: build\n    if: github.repository != 'phranck/velvet-template' && needs.build.result == 'success'\n    name: Deploy to GitHub Pages\n    runs-on: ubuntu-latest\n    permissions:\n      pages: write\n      id-token: write\n    environment:\n      name: github-pages\n      url: ${{ steps.deploy.outputs.page_url }}\n    steps:\n      - name: Deploy to GitHub Pages\n        id: deploy\n        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128\n"
  }
} as const;
